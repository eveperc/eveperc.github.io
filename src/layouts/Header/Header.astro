---
import Header from "./Header";
import HeaderHome from "./Header.Home";
import HeaderSearch from "./Header.Search";

type Props = {
  url: string;
};

const { url } = Astro.props;
---

<Header>
  <HeaderHome/>
  <HeaderSearch url={url}/>
</Header>
